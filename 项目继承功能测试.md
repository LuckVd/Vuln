# 项目继承功能测试指南

## 功能概述

项目继承功能允许一个项目继承另一个项目的配置、问题单、附件等资源，支持以下特性：

1. **多种继承类型**：完全继承、仅配置继承、仅问题单继承
2. **循环继承检测**：防止形成循环继承关系
3. **继承记录追踪**：记录所有继承操作历史
4. **灵活过滤条件**：支持按状态和漏洞等级过滤继承的问题单
5. **继承状态管理**：实时显示继承进度和状态

## 测试步骤

### 1. 访问项目列表页面
- 打开浏览器访问 `http://localhost:8001`
- 点击左侧导航菜单中的 "项目管理"
- 查看项目列表，新增了 "继承关系" 列显示继承状态

### 2. 创建项目继承关系
1. 在项目列表中找到 "PRJ-003"（状态为"已创建"的项目）
2. 点击操作列中的 "继承" 按钮
3. 在弹出的继承弹窗中：
   - 选择父项目：选择 "PRJ-001" 或其他项目
   - 选择继承类型：选择 "完全继承"
   - 勾选继承内容：问题单、附件、快照
   - 设置过滤条件（可选）：选择特定状态和漏洞等级
4. 点击 "确认继承" 按钮

### 3. 查看继承状态
- 项目列表中的 "继承关系" 列会显示父项目编号和继承状态
- 初始状态为 "继承中"，3秒后会变为 "已继承"

### 4. 查看项目详情中的继承信息
1. 点击项目编号进入项目详情页面
2. 在 "基本信息" Tab 中可以看到：
   - 父项目信息（带跳转链接）
   - 继承状态标签
   - 继承时间
   - 子项目数量
3. 在 "继承关系" Tab 中可以查看：
   - 父项目详情卡片（可取消继承）
   - 子项目列表（如果有）
   - 继承记录表格

### 5. 测试继承功能限制
- 已关闭项目不能设置继承关系
- 有子项目的项目不能取消继承关系
- 不能形成循环继承（A继承B，B不能继承A）

## API 接口说明

### 新增的 API 接口：

1. **获取可继承项目列表**
   ```
   GET /api/project/inheritable/:projectId
   ```

2. **创建项目继承关系**
   ```
   POST /api/project/:id/inherit
   Body: {
     parentProjectNumber: string,
     inheritanceType: 'full' | 'config' | 'problems',
     copyProblems?: boolean,
     copyAttachments?: boolean,
     copySnapshots?: boolean,
     problemFilter?: {
       status?: number[],
       vulnerabilityLevel?: number[]
     }
   }
   ```

3. **获取项目继承关系**
   ```
   GET /api/project/:id/inheritance
   ```

4. **获取项目继承记录**
   ```
   GET /api/project/inheritance-records
   ```

5. **取消项目继承关系**
   ```
   DELETE /api/project/:id/inheritance
   ```

## 数据模型扩展

### Project 接口新增字段：
```typescript
interface Project {
  // 原有字段...
  parentProjectNumber?: string;        // 父项目编号
  inheritanceType?: 'none' | 'full' | 'config' | 'problems';
  inheritanceStatus?: 'pending' | 'completed' | 'failed';
  inheritedFromProject?: string;       // 继承源项目编号
  inheritedAt?: string;               // 继承时间
}
```

### 新增接口：
```typescript
interface ProjectInheritanceParams {
  parentProjectNumber: string;
  inheritanceType: 'none' | 'full' | 'config' | 'problems';
  copyProblems?: boolean;
  copyAttachments?: boolean;
  copySnapshots?: boolean;
  problemFilter?: {
    status?: number[];
    vulnerabilityLevel?: number[];
  };
}

interface ProjectInheritanceRecord {
  id: number;
  fromProjectNumber: string;
  toProjectNumber: string;
  inheritanceType: 'none' | 'full' | 'config' | 'problems';
  inheritedItems: {
    problems?: number;
    attachments?: number;
    snapshots?: number;
  };
  status: 'pending' | 'completed' | 'failed';
  createTime: string;
  completeTime?: string;
  errorMessage?: string;
}
```

## 界面功能特点

### 1. 项目列表页面
- 新增 "继承关系" 列显示继承状态
- 新增 "继承" 操作按钮（已关闭项目禁用）
- 继承状态标签显示（继承中、已继承、继承失败）

### 2. 项目继承弹窗
- 父项目选择下拉框（支持搜索）
- 继承类型选择（完全继承、仅配置继承、仅问题单继承）
- 继承内容勾选（问题单、附件、快照）
- 问题单过滤条件（状态、漏洞等级）

### 3. 项目详情页面
- 基本信息显示继承相关信息
- 专门的 "继承关系" Tab 页
- 父项目信息卡片（带取消继承功能）
- 子项目网格展示
- 继承记录历史表格

## 预期效果

通过项目继承功能，用户可以：

1. **快速创建相似项目**：基于现有项目快速创建配置相似的新项目
2. **配置复用**：继承父项目的问题分类、处理流程等配置
3. **问题单管理**：选择性继承父项目的问题单，避免重复录入
4. **项目管理**：建立项目层级关系，便于管理和查看
5. **历史追踪**：完整的继承操作记录，便于审计和回溯

## 注意事项

1. 继承操作是异步执行，状态会实时更新
2. 循环继承检测确保数据关系的一致性
3. 取消继承关系会检查是否有子项目依赖
4. 所有继承操作都有完整的记录和状态追踪

现在可以打开浏览器访问 `http://localhost:8001/project` 来测试项目继承功能了！